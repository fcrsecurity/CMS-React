import React from 'react'
import TitleWidget from '../widgets/TitleWidget'

export default (initialProps) => {
    return (
        <div>
            <TitleWidget {...initialProps} />
        </div>
    )
}
