import React from 'react'
import PressReleasesWidget from '../widgets/PressReleasesWidget'

export default (initialProps) => {
    return (
        <div>
            <PressReleasesWidget {...initialProps} />
        </div>
    )
}
