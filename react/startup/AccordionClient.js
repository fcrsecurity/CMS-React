import React from 'react'
import AccordionWidget from '../widgets/AccordionWidget'

export default (initialProps) => {
    return (
        <div>
            <AccordionWidget {...initialProps} />
        </div>
    )
}
