import React from 'react'
import SliderInfoWidget from '../widgets/SliderInfoWidget'

export default (initialProps) => {
    return (
        <div>
            <SliderInfoWidget {...initialProps} />
        </div>
    )
}
