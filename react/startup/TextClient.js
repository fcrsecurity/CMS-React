import React from 'react'
import TextWidget from '../widgets/TextWidget'

export default (initialProps) => {
    return (
        <div>
            <TextWidget {...initialProps} />
        </div>
    )
}
