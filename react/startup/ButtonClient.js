import React from 'react'
import ButtonWidget from '../widgets/ButtonWidget'

export default (initialProps) => {
    return (
        <div>
            <ButtonWidget {...initialProps} />
        </div>
    )
}
