import React from 'react'
import ResultWidget from '../widgets/ResultWidget'

export default (initialProps) => {
    return (
        <div>
            <ResultWidget {...initialProps} />
        </div>
    )
}