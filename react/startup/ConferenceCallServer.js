import React from 'react'
import ConferenceCallWidget from '../widgets/ConferenceCallWidget'

export default (initialProps) => {
    return (
        <div>
            <ConferenceCallWidget {...initialProps} />
        </div>
    )
}
