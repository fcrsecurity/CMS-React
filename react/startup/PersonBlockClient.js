import React from 'react'
import PersonBlockWidget from '../widgets/PersonBlockWidget'

export default (initialProps) => {
    return (
        <div>
            <PersonBlockWidget {...initialProps} />
        </div>
    )
}
