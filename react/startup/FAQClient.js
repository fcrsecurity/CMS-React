import React from 'react'
import FAQWidget from '../widgets/FAQWidget'

export default (initialProps) => {
    return (
        <div>
            <FAQWidget {...initialProps} />
        </div>
    )
}
