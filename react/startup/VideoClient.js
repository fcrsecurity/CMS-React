import React from 'react'
import VideoWidget from '../widgets/VideoWidget'

export default (initialProps) => {
    return (
        <div>
            <VideoWidget {...initialProps} />
        </div>
    )
}
