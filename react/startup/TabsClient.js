import React from 'react'
import TabsWidget from '../widgets/TabsWidget'

export default (initialProps) => {
    return (
        <div>
            <TabsWidget {...initialProps} />
        </div>
    )
}
