import React from 'react'
import TeamTabsWidget from '../widgets/TeamTabsWidget'

export default (initialProps) => {
    return (
        <div>
            <TeamTabsWidget {...initialProps} />
        </div>
    )
}
