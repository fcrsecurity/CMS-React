{% set widget = widget|merge({'data' : checkWidgetFields(widget.data, fields) }) %}

{{ react_component(
	widget.dataType, {
		'props':{
			"isLoggedIn": is_granted('ROLE_USER'),
			"id": widget.id,
			"items": investors_press_releases_latest(3),
			"config": widget.config
		}
	}
) }}
